/** This identity names the actual checked-in prompt, not a deployment label. */
export const SUPPORTED_MODEL_PROMPT_VERSION = '1' as const;
export const DEFAULT_MODEL_OUTPUT_TOKENS = 16000;
export const MAX_CONFIGURED_MODEL_OUTPUT_TOKENS = 262144;
