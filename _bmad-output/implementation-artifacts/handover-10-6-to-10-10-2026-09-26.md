# Handover: Stories 10.6 to 10.10 (paused 2026-09-26)

**Any agent that continues Stories 10.6 to 10.10 starts here.** This file is identical on all five
`claude/10-*` branches.

## 1. Which branches to continue

The owner chose these branches on 2026-09-26. Continue **only** on them. Do not start a new
implementation of any of these stories.

| Story | Branch | PR | PR base | State |
|---|---|---|---|---|
| 10.6 | `claude/10-6-legacy-visibility` | #61 | `main` | Built; work in progress |
| 10.7 | `claude/10-7-live-channel` | #60 | `main` | **Done**: reviewed, CI green, waits for the owner |
| 10.8 | `claude/10-8-lost-connection` | #62 | `main` | Built and reviewed; work in progress |
| 10.9 | `claude/10-9-replay-bounded-history` | #63 | `claude/10-6-legacy-visibility` | Built; not reviewed |
| 10.10 | `claude/10-10-retained-decision-history` | #64 | `claude/10-6-legacy-visibility` | Built; not reviewed |

**Superseded: do not continue, merge or copy from these** (another agent built them in parallel
while this work was paused; the owner kept the branches above):

| Story | Superseded branch | PR | Why not |
|---|---|---|---|
| 10.6 | `codex/story-10-6` | #59 | Red CI; wording the owner did not approve |
| 10.7 | `codex/story-10-7-live-channel` | #55 | No self-refresh filter: with the new notify, an open Evidence page re-reads itself about once a second on an active Run, and each re-read writes two permanent audit events |
| 10.8 | `codex/story-10-8` | #56 | The route-boundary half is not built, and there is no heartbeat on connect, so a quiet, healthy stream reads "No update" around every planned 14-minute renewal |
| 10.9 | `codex/story-10-9` | #57 | Wording the owner did not approve |
| 10.10 | `codex/story-10-10` | #58 | Wording the owner did not approve |

Rules that still hold: never merge, deploy or push to `main`; push only to the branch of the story
you work on; never rewrite pushed history (merge, do not rebase); never weaken, skip or delete a
test; the `<frozen-after-approval>` blocks stay unchanged.

## 2. Owner decisions on wording

The frozen specs reserve all new wording for the owner. Two sheets exist.

### 2.1 Sheet 1: APPROVED by the owner on 2026-09-26 ("approve all")

Use these sentences exactly. Where code still marks them "PROPOSED" (Story 10.6's words
modules), remove the mark, except for the sentences that sheet 2 changes.

#### 10.6 — Old screens show what was recorded

##### Pauses and resumes (Execution Timeline, and the Paused banner)

| Where | Sentence |
|---|---|
| Section heading | Pauses and resumes |
| Section intro | Each pause says where it held the Run, and each resume says which attempt it started. A resume restarts the held step as a new attempt; the attempt a pause interrupted is marked superseded. |
| Entry title | Pause {n} |
| Who paused | Paused by {name} at {time}. |
| Who resumed | Resumed by {name} at {time}. |
| Pause during an attempt | Held at {step}, during attempt {n}. That attempt was superseded. |
| Pause between steps | Held before {step}. No Step Execution was in flight. |
| Pause after an inspection | Held after the inspection of {record} finished, before {step}. No Step Execution was in flight. |
| What the resume started | It restarted {step} as a new attempt (attempt {n}). |
| Old record, no step | The step this pause held the Run at was not recorded. |
| Old record, no attempt | The attempt this resume started was not recorded. |
| Resume, no attempt yet | This resume has not started a new attempt of the held step. |
| Still paused | The Run is still paused. |
| Deadline passed | This pause reached its deadline at {time} without a resume. |
| Run ended during pause | This pause was withdrawn at {time}, when the Run ended. |
| Record unreadable | How this pause ended could not be read. |
| Long list | Showing the first {shown} of {total} pauses. |
| Paused banner, now | The Run is held at {step}. Attempt {n} was superseded; Resume restarts that step as a new attempt. |
| Paused banner, now | The Run is held before {step}. |
| Paused banner, old record | The step this pause holds the Run at was not recorded. |
| Paused banner, read failed | Where this pause holds the Run could not be read. Reload the Run. |

`{step}` reads like: “Inspect the record” for E-000102 on LoanCore.

##### A record a person matched (Result, record review, inspector, Exceptions)

| Where | Sentence |
|---|---|
| Flag (existing word) | Human-matched |
| Section heading | Records a person matched |
| Section intro | A person chose which account on the Target System each of these records is, by answering an Escalation. The Result stands on those choices. |
| The answer | chose candidate {n} of {m} |
| Label beside the Agent's text | the candidate the person chose, as the Audit Agent described it |
| Old record, no link | The decision that matched this record is not linked to it, so who chose it and when cannot be shown. |
| Technical details label | Escalation that matched it |
| Long list | Showing the first {shown} of {total} records a person matched. |

##### Replay gaps

| Where | Sentence |
|---|---|
| Rail section heading | Gaps in this playback |
| Summary line | Playback is incomplete: {n} frames are missing. (1 frame: "1 frame is missing.") |
| Disclosure | Where the gaps are |
| One gap | Missing frame · after frame {n} (or "before the first frame") · {what the step did} |
| Long list | Showing the first {shown} of {total} gaps. |

##### Live View, service steps

| Where | Sentence |
|---|---|
| Step record unreadable | The artifact record for this step could not be read, so its digest is not shown. Reload the page to try again. |

---

#### 10.8 — Lost connection and lost answer

The page that shows when a page fails to load. It no longer says "Nothing was changed."

| Where | Sentence |
|---|---|
| Heading (unchanged) | This page could not be loaded |
| Run pages | This page could not be loaded. Check the Run's current state before repeating your last action. (Your own candidate sentence from epics.md.) |
| Other pages | This page could not be loaded. Check the current state before repeating your last action. |
| Body | Reload this page to read it again. Reloading from here does not repeat your last action. If the page keeps failing, tell a PoC Administrator. |
| Button (a plain link) | Reload this page |

---

#### 10.9 — Replay says what a long list covers

| Where | Sentence |
|---|---|
| Jump list, Escalations | Showing the first {shown} of {total} Escalations. |
| Jump list, Exceptions | Showing the first {shown} of {total} Exceptions. |
| How to reach the rest | To see one of the rest, open its record in the record review and choose Replay. |

The Observation count beside each frame: I recommend an **exact** count (read from the
database for each frame). Then the existing sentence "{count} had been registered when this
screen was captured." stays true and no new sentence is needed.

---

#### 10.10 — Decisions on the Execution Timeline

##### Answered Escalations (a new section, beside "Pauses and resumes")

| Where | Sentence |
|---|---|
| Section heading | Escalation answers |
| Section intro | Each Escalation a person answered: the answer, who gave it, and when. |
| Entry title | The kind, in the existing words: Choose candidate / Unnamed value / Retry or skip |
| Who answered | Answered by {name} at {time}. |
| The answer, candidate | Answer: chose candidate {n} of {m}. |
| The answer, fixed option | Answer: {option}. (The platform's own option words: Retry, Skip, Abort, Mark the record ambiguous, Mark the record Unevaluated and continue.) |
| Abort | The Run was canceled by this answer. |
| Where it was raised | Raised at {step}. |
| Old record, no step | The step this Escalation was raised at was not recorded. |
| Link | Open in Replay |

The question and the candidate text that the Audit Agent wrote are not shown as the
platform's words. At most they show in the existing "untrusted content" box.

##### A pause the Run never reached (added to "Pauses and resumes")

| Where | Sentence |
|---|---|
| Entry title | Pause request |
| Who asked | Requested by {name} at {time}. |
| What happened | The Run ended before the pause took effect, so its own outcome stands. |

The last sentence copies the existing cancellation sentence: "The Run ended before the
cancellation was performed, so its own outcome stands."

The same entry also covers a "pause after this inspection" request that the Run ended
before (the same kind of record, from the workspace).

### 2.2 Sheet 2: PENDING the owner's decision (sent 2026-09-26)

Until the owner approves, keep these as proposed: each sentence in its words module, pinned by a
test that reads it back, and listed as "needs owner confirmation". If the owner changes a row, use
the owner's words.

#### Part A: 10.6, sentences changed after the screenshot review

| # | Where | Approved this morning | Now | Why |
|---|---|---|---|---|
| A1 | "Pauses and resumes", intro | Each pause says where it held the Run, and each resume says which attempt it started. A resume restarts the held step as a new attempt; the attempt a pause interrupted is marked superseded. | Each pause says where it held the Run, and each resume says which attempt it started. When a pause interrupted an attempt, that attempt is marked superseded and the resume restarts the step as a new attempt. | A pause held between two steps interrupts nothing. |
| A2 | A resume after a pause held **between** steps | *(none; only "It restarted {step} as a new attempt (attempt {n}).")* | It started {step} (attempt {n}). | "Restarted" was false: that step had not begun. "Restarted" stays for an interrupted attempt. |
| A3 | Paused banner | The Run is held at {step}. Attempt {n} was superseded; Resume restarts that step as a new attempt. | The Run is held at {step}, during attempt {n}. That attempt was superseded. | "Attempt {n}" read as if it was the new attempt. |
| A4 | Paused banner, last line (one of three) | *(the old line: "The agent restarts the current Step from its first Tool Action.")* | a) Evidence already collected is preserved. Resume restarts that step from its first Tool Action as a new attempt.<br>b) Evidence already collected is preserved. Resume starts that step from its first Tool Action.<br>c) (the record does not say) Evidence already collected is preserved. The agent restarts the current Step from its first Tool Action. | The old line said "restarts" even when nothing had started. |
| A5 | A step the plan cannot name | *(none)* | plan step “{id}” | A fallback, so the sentence never has a hole in it. |

**Also, one yes/no (10.6):** new **optional** fields on existing audit events. There is no new event type and no migration. The fields: `humanMatchDecisions`, `planStepId`, `heldWorkItemId`, `stepExecutionId`, `attempt`, `resumedWaitId`. Old events stay as they are. I recommend **yes**.

---

#### Part B: 10.10, new sentences for rare cases

| # | When it shows | Sentence |
|---|---|---|
| B1 | The Work Item was not recorded | The Work Item this Escalation was raised for was not recorded. |
| B2 | The answer cannot be read | The answer this Escalation received could not be read. |
| B3 | A long list | Showing the first {shown} of {total} Escalation answers. |
| B4 | A "pause after this inspection" request that an immediate pause replaced | A request to pause the Run at once replaced it before it took effect. |
| B5 | Why a pause request did not take effect cannot be read | Why this pause request did not take effect could not be read. |
| B6 | A "pause after this inspection" request | It asked to pause after {step}. |
| B7 | The inspection is not recorded | The inspection it asked to pause after was not recorded. |
| B8 | No time recorded | Requested by {name}. When it was requested was not recorded. |
| B9 | No name recorded | Requested at {time}. Who requested it was not recorded. |
| B10 | Neither recorded | Who requested this pause, and when, was not recorded. |
| B11 | A long list | Showing the first {shown} of {total} pause requests. |
| B12 | Replay, opened from "Open in Replay" | Opened at the Escalation “{kind}”. |
| B13 | Replay, when the Escalation has no frame | Opened for the Escalation “{kind}”: {why there is no frame}. |
| B14 | Replay, when the linked Escalation is not in this view | The Escalation this link names is not available in this Replay view. |
| B15 | Replay, after B13 or B14 (only when a target with a frame exists) | Choose a recorded target below. |

B4 matters most: the approved "The Run ended before the pause took effect…" is **false** in that case, because the Run did not end.

## 3. Where each story stands, and what is next

The build workflow (`_bmad/render/bmad-build-auto/intellifin-audit-7ffb6dd68acd/c64af01da8bc52cc04e7/`)
applies: after the build, a review by four reviewers over the diff since the spec's
`baseline_revision` (step-04-review.md and `review-prompts/`), triage, fixes, verification, then
`## Review Triage Log`, the `deferred:` list, `## Auto Run Result`, `followup_review_recommended`
and `status: done` in the spec. Then commit, push, and take "WIP" off the PR.

**The owner's bar for screens:** before a story is called done, look at a screenshot of every new
or changed UI state (1280x800, and 1024 wide for the Timeline and Replay), as an auditor would.
Fix spacing, alignment, heading level, wrapping of long values, focus rings, and any sentence that
is not true for that state. Use only existing tokens and component classes, with no new colours.

### 10.6 (PR #61), head `14b51706`
- Built. Full browser suite 291 passed at `d8734c1f`. Integration 791 of 792 (the flake in section 5).
- Commit `4f6aacd5` fixed sentences the screenshots showed untrue (sheet 2, part A).
- Next: (1) `pnpm typecheck` and `pnpm test` (the pushed commit `d2f461a8` keeps its "WIP:"
  message: never rewrite pushed history; record the result in the story record instead); (2) screenshots of every new state again;
  (3) CLAUDE.md notes (screenshot lessons; Chromium breaks `E-000102` after the hyphen, so keep
  record keys in an `ls-nowrap` span; `completeRun` does not move a Run to its terminal state);
  (4) fold the "Record draft" at the end of the spec into its record; (5) the four-reviewer review
  and its fixes; (6) remove the PROPOSED marks for sheet-1 sentences; (7) finalize and take "WIP"
  off #61.
- For the owner (listed in sheet 2): new optional payload keys on existing event types
  (`humanMatchDecisions`, `planStepId`, `heldWorkItemId`, `stepExecutionId`, `attempt`,
  `resumedWaitId`). No new event type, column or migration.

### 10.7 (PR #60), head `6bbe31e0`
- Done. Nothing to continue unless review comments arrive.

### 10.8 (PR #62), head `5e5f7976`
- Built; the four-reviewer review is done and its triage log and deferred list are in the spec.
  All 12 review fixes are committed. The "take the larger cursor" part of one fix was left out on
  purpose: it can skip the frame that says the Run ended (CLAUDE.md, Story 10.8 note).
- Next: (1) browser verification of the connect heartbeat, the boundary (`route-boundary.spec.ts`,
  the flag-run lost-acknowledgement leg on an address with a query), the hand-off and
  `live-drop.spec.ts`, and of the screenshot fixes in `110c3baa` (`LiveGateNote`, button
  underlines, toolbar row, flag opener size); (2) root typecheck, `pnpm boundaries`, `pnpm test`;
  (3) fold the "Record update draft" at the end of the spec into its record; (4) finalize
  (`## Auto Run Result`; follow-up review recommended, since 1 medium and 11 low were patched) and
  take "WIP" off #62.
- Seen and not changed: the session chrome still says LIVE while the stream is `lost`; the
  Workspace "Replay" and "Approved procedure" links are 4px apart.

### 10.9 (PR #63), head `880ecf33`
- Built and tested (unit 5,508; 8 integration files, 76 tests; the Replay browser specs); the
  screenshot review is done.
- Next: (1) merge `claude/10-6-legacy-visibility` into it (merge commit); (2) the four-reviewer
  review over the diff from the 10.6 head it builds on; (3) full integration and browser suites;
  (4) finalize.
- Named, out of scope: on a Run with more than 500 frames, the default view finds each frame's
  Tool Action through the Timeline's first 500 by time, so late frames can wrongly say "No Tool
  Action was recorded for this frame" (fix: read by frame ids).

### 10.10 (PR #64), head `3ffab528`
- Built and tested (unit 5,539; `decision-history.test.ts` 6 of 6; five browser spec files); the
  screenshot review is done. Its reads are in a new `decision-history.ts`.
- Next: (1) the owner's decision on sheet 2, part B; (2) merge `claude/10-6-legacy-visibility`,
  then 10.9 (conflicts expected in `replay.ts`, the Replay page, `ReplayViewer.tsx`,
  `replay-v1.md`); (3) full integration and browser suites; (4) the four-reviewer review;
  (5) finalize.

## 4. Merging these branches

- 10.6 merges first: 10.9 and 10.10 are stacked on it.
- 10.7 and 10.8 both edit `docs/contracts/live-timeline-channel-v1.md` and `CLAUDE.md`.
  Whichever merges second reconciles them and re-runs the channel tests (10.8 adds a heartbeat on
  connect, in new test files).
- Every branch adds a note at the top of `CLAUDE.md`: keep all of them.
- `sprint-status.yaml`: keep each story's own line and the latest `last_updated`.

## 5. Known flakes (not caused by these stories)

- `tests/integration/agent-isolation.test.ts`, "keeps concurrent authenticated Runs on the same
  origin in separate cookie and storage contexts": fails under load, also at `429e08c`. Cause
  found: a navigation the scope guard blocks later loads `chrome-error://chromewebdata/`, which
  stops the next `goto` on the reused page (`unavailable`). Fix idea: discard the page after a
  scope refusal, or wait for the error page before reusing it.
- `tests/integration/record-review.test.ts`, "admits concurrent cursorless reads": failed once
  under load, passes alone.
