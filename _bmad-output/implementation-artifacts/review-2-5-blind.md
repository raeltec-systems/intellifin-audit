- `packages/infrastructure/drizzle/0011_quick_nighthawk.sql:15` backfills `platformCaptured: true` for every P-1 version from Template identity rather than its persisted targets. A P-1 Draft whose web/desktop targets were removed in Story 2.3 acquires incorrect capture metadata. Derive the flag from the row's current target snapshots and cover an edited-target migration fixture.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:318` refreshes the Schedule's concurrency token without refreshing its frequency or start time. When refreshed server props contain another author's Schedule, this editor can retain old values while obtaining permission to overwrite the new values. Reconcile pristine inputs with server props and preserve the original token or report a conflict for dirty inputs.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:73` likewise adopts every new row token while deliberately preserving dirty Evidence Requirements at lines 78–86. A server refresh after a concurrent evidence edit silently rebases the local full-array replacement over the other author's changes. Track the evidence baseline and require conflict resolution when that section changed remotely.
- `packages/domain/src/procedures/evidence-draft.ts:267` refuses an empty grounding array before the command can derive platform Structural Snapshot capture from the selected targets. The UI compensates by manufacturing that grounding in the request, making otherwise identical command callers behave differently. Keep input shape checks before the lock, but apply the grounding invariant after authoritative capture derivation.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:80` clears dirty state only when the submitted local array exactly matches the persisted array. If Target selection forces Structural Snapshot/screenshot while an existing evidence edit is dirty, save normalizes the payload but leaves the local array unchanged; the successful save can therefore leave dirty state stuck. Reconcile the acknowledged normalized submission without dropping edits made during the request.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:395` and `:414` do not clear the previous save result when Schedule fields change. The surface continues saying the Schedule is saved while displaying newly edited, unsaved values. Clear that result or explicitly mark the editor unsaved on both changes.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:413` marks the start-time input invalid when the actual error is a missing frequency, while the frequency select never receives `aria-invalid`. Associate each validation failure with the field that needs correction so assistive technology does not direct the user to an already valid time.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:199` uses array indices for row identity and removes rows without transferring focus. Removing the final focused row drops keyboard focus out of the editor; removing an earlier row can associate touched state with a different requirement. Give authoring rows stable client IDs and move focus to the next surviving row or Add button.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:117` checks only blank names and grounding locally, so duplicate names proceed through the confirmation dialog and fail as a generic server banner. Detect duplicates with the domain's trim/case rules before confirmation and identify the conflicting rows inline.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:420` exposes internal hyphenated period-rule identifiers and the implementation statement that a Procedure Version never runs the rule. Render the auditor-facing meaning, such as “Previous Monday through Sunday, in UTC,” while keeping the stored identifier internal.
- `apps/web/src/procedures/EvidenceScheduleForm.tsx:144` and `:346` promise “Nothing was changed” for any rejected action promise. A response can be lost after the database transaction commits, so that statement is not established. Use an outcome-unknown message and refresh the saved state before inviting a retry; add a test for a committed save followed by a lost response.
- `tests/integration/procedures.test.ts` verifies the backfill by reading SQL text and comparing Template defaults, but does not execute migration 11 against generation-10 rows with modified targets and different lifecycle states. Add an actual upgrade fixture to validate preserved authoring and capture metadata; the current text assertion cannot catch the incorrect P-1 backfill above.
