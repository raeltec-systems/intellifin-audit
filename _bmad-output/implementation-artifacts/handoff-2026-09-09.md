---
title: 'Handoff: Epic 4 closed on its final candidate, the hero workflow, and Epic 5 begun'
type: 'handoff'
created: '2026-09-09'
status: 'final'
---

# Handoff, 2026-09-09

The owner asked for six things to be stated. Each is below, in that order.

## 1. Decisions implemented

| Decision | Status | Where |
|---|---|---|
| 1. C2 role-privilege policy as a frozen condition binding | done, live-proven | `1dee0cb`; live runs `34299424112` and `34317015975` |
| 2. D3, the 24-hour window through the complete evidence path (one bounded story) | done; Builder authoring landed with the hero pass | `be91d3e` (Story 4.12), `7a8f372`; journeys `disablement-window-journey.spec.ts`, `hero-workflow.spec.ts` |
| 3. Merge PR 24 as the combined release | engineering side done: containment of PR 23 confirmed (`665c5a3` is an ancestor, zero commits missing), the exact final candidate is green in CI and live acceptance, no temporary transport workflow remains; the merge itself waits for the owner (section 5) | `epic-4-deployment-readiness.md` §4 |
| 4. A refused Target System stays in the Result | done | `db3c6d3` |

## 2. Epic 4 acceptance and deployment status

- **Final candidate** `9da4df6` (the merge of the hero-workflow pass); head `da90ef5` is
  docs-only with the identical application tree.
- **Hosted CI** `34316930519`: all five jobs green on `9da4df6`.
- **Live acceptance** `34317015975` on `9da4df6`: the policy-bound audit (COMPLETED,
  PENDING_CONFIRMATION, a consistent proposal retained for human review), the
  undefined-privilege negative case (waited durably, cancelled, released) and remote
  isolation, on Solari and OpenAI against the hosted Northstar; secret-free artifact
  `10090483703`.
- **Populated upgrade** from production's generation 14 on main's own data: every
  pre-existing row unchanged, exact parity with a fresh generation-41 install,
  idempotent migrator (`epic-4-populated-upgrade-proof.md`).
- **Story status by level** (implemented, locally verified, remotely verified,
  owner-reviewed, deployed): `epic-4-story-status.md`. Every story is at the first three
  levels; none is owner-reviewed or deployed.
- **Deployment**: blocked on the owner inputs in `epic-4-deployment-readiness.md` §2
  (names only). Without the worker inputs every Run fails closed with a stated reason.
  The post-merge inspection list is §5 of the same document.

## 3. Procedure-setup improvements, demonstrated

`epic-4-hero-workflow-report.md` with sixteen screenshots in `hero-ux-screenshots/`, each
a state the browser journey proved: editable Drafts that say so; direct section saves
with the confirmation kept for submit, approval, activation, scope expansion and
cancellation; P-1's C1 authored in business language over the one saved rule; the
24-hour window as a third condition beside C1 with its readiness items and the offered
`disabled_time` requirement; readiness before paid execution as advice; an agent summary
derived from the frozen plan; honest lost-response recovery. Section 5 of that report is
the walk-through.

## 4. Epic 5 progress

Branch `codex/epic-5-live-replay` from `9da4df6`; PR 25, stacked on PR 24 and targeting
`main` because CI runs only there. Story 5.1 (the live Timeline channel: SSE per Run and
per list, LISTEN/NOTIFY, cursor replay with no gap and no duplicate, heartbeat, planned
lifetime, abort teardown; Run Detail, the Runs list and the bell subscribing; the stale
indicator) is implemented and locally verified at every level (`3a426a4`); hosted CI is
PR 25's checks. Context, order and the bar for done: `epic-5-context.md`. Next: 5.3 Live
View, 5.7, 5.2, 5.8, 5.4, 5.5, 5.6.

## 5. Exact commits and blockers

On `codex/epic-4-agent-runs` (all pushed): `1dee0cb`, `db3c6d3`, `c969206`, `ad238db`,
`be91d3e`, `6237c4c`, `7f11131`, `85d2f1c`, `07f79e2`, `d1d9ade`, `efa6625`, the hero
branch (`b8a2bb3`, `3b7fad5`, `56219b0`, `f702c6c`, `ef70035`, `806fbe7`, `7a8f372`,
`e2d19c2`, `5aaad10`, `a09fb99`, `34e35dd`), the merge `9da4df6`, and `da90ef5`.
On `codex/epic-5-live-replay`: `3a426a4`.

Blockers, all the owner's:
- Worker variables on Railway (`EVIDENCE_S3_*`, `CREDENTIAL_TOKENS`, a fresh
  `EXCEPTION_FINGERPRINT_KEY`, a ROTATED `SOLARI_API_KEY` — the one pasted into chat is
  exposed — `SOLARI_RECORDING=false`, the `AGENT_*` model choice); `CREDENTIAL_CAPABILITIES`
  verified on the web service; the stale `SCHEMA_RANGE_*` removed.
- The review and merge of PR 24 (merge commit), then closing PR 23 as superseded; a
  required-status-check rule on the unprotected `main` is recommended first.
- After the release: seed Northstar into production, give LoanCore the `Disabled time`
  label, author the C2 policy on a P-1 version, run one representative audit.

Nothing was merged, no branch was force-pushed, no secret was printed or committed, no
fingerprint key was rotated, no test was weakened.

## 6. How the owner can test

1. **The hero workflow**: `epic-4-hero-workflow-report.md` §5, six steps from New
   Procedure to Submit, or run
   `HERO_UX_SCREENSHOTS=<folder> pnpm exec playwright test tests/e2e/hero-workflow.spec.ts`
   for fresh pictures.
2. **The live channel** (PR 25): open a queued Run's detail in one tab and cancel it from
   another; the first tab shows Canceled within five seconds without a reload and then
   carries the plain `Updated … Refresh.` banner. Block `/api/runs/*/events` in the
   browser's devtools to see the stale indicator after fifteen seconds. Or run
   `pnpm exec playwright test tests/e2e/live-timeline.spec.ts`.
3. **After the release**: the five post-merge checks in
   `epic-4-deployment-readiness.md` §5.
