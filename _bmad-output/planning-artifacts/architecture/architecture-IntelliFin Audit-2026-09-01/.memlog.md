---
scope: IntelliFin Audit exploratory PoC and reusable commercial-product foundations
purpose: build-substrate
altitude: feature
updated: 2026-09-01T15:55
---

- (decision by user) Use the Coaching path and produce only the terse build-oriented architecture spine.
- (constraint) Greenfield repository: no application code or existing technical conventions are present to ratify.
- (constraint) PoC is a web application with background Audit Runners, four preconfigured Procedures, synthetic data, and read-only Source access.
- (constraint) Deterministic rules own record classification and immutable System Outcomes; agentic behavior is bounded to controlled acquisition or extraction and must fail safely.
- (constraint) Evidence provenance, exact population reconciliation, Evidence Quality Gate, immutable Run history, and separate human review state are binding product invariants.
- (constraint) ProdConsole configuration testing is the PoC agentic vertical slice; other Sources use real synthetic file or read-only API acquisition.
- (constraint) Architecture must preserve a future private or customer-hosted Audit Runner boundary without implementing that deployment in the PoC.
- (constraint) UX mental model is Procedure → Run → Evidence → Result → Auditor Review; agent execution detail remains subordinate.
- (decision by user) Adopt TypeScript as the primary language and Next.js for the web UI; Python is permitted only for a compelling isolated data-processing need, not as a distributed default.
- (direction by user) Optimize for a very small human-plus-coding-agent team: speed, clarity, testability, safe modification, and low operational overhead.
- (decision by user) Adopt a modular monolith in a monorepo with separately runnable web and background worker processes; avoid microservices and Kubernetes.
- (decision by user) Audit Runner is a first-class execution component, never synchronous request work; it must support one genuine bounded agentic web-acquisition flow.
- (decision by user) Use Solari as the initial browser/computer-use/sandbox provider behind a clean provider boundary that preserves future private or customer-hosted execution.
- (decision by user) Adopt PostgreSQL and a relational domain model for Procedures, Runs, Evidence, Exceptions, Reviews, and audit history; avoid disposable persistence shortcuts.
- (constraint) PoC deployment is cloud-hosted, container-friendly, synthetic-data-only, publicly demoable, and must support web, PostgreSQL, long-running workers, logs, secrets, and low operational overhead.
- (direction by user) Do not introduce NestJS merely from familiarity; use a separate lightweight TypeScript backend only if runner boundaries require it.
- (constraint) Manual Runs only; four preconfigured Procedures; read-only Sources; deterministic evaluation authoritative; uncertain agent or Evidence states fail safely.
- (question) Research and decide: hosting, background jobs, authentication, ORM/data access, LLM provider boundary, Next.js-only versus separate API, monorepo tooling, and observability.
- (version) Verified 2026-09-01 paved-path baseline: Node.js 24 LTS; Next.js 16.3.4; React 19.2.8; PostgreSQL 18.6; pnpm 11.25.0.
- (direction) Research favors a ports-and-adapters modular monolith: Next.js is an inbound web adapter and composition root; a separate Node worker is an inbound job adapter; shared domain/application packages remain framework-independent; no separate HTTP API service for the PoC.
- (direction) Research favors pg-boss 12.29.0 for PoC jobs: durable PostgreSQL-backed execution, retries, heartbeats, concurrency and transactional enqueue without Redis or an external workflow control plane; mitigate library/at-least-once side-effect risk with application idempotency and explicit stage checkpoints.
- (direction) Research favors Drizzle ORM 0.45.2 plus drizzle-kit 0.31.10 and explicit SQL migrations: TypeScript schema, visible SQL, low runtime weight, and less generated-client/tooling surface than Prisma 8's newly changed migration path.
- (direction) Research favors Better Auth 1.7.2 with PostgreSQL-backed sessions for PoC portability; keep product authorization roles and review permissions in the application domain rather than coupling them to an identity vendor.
- (direction) Research favors pnpm workspaces without Turborepo initially: two apps and a few shared packages do not yet justify a second task-orchestration layer; add one only when build graph or cache cost becomes material.
- (direction) Research favors Railway for the PoC: native Next.js/Docker, monorepo services, long-running processes, managed PostgreSQL, secrets, logs, public domains, and private S3-compatible buckets in one low-operations project; preserve Docker and S3/PostgreSQL portability.
- (direction) Evidence binary artifacts should use private S3-compatible object storage behind an EvidenceObjectStore port; PostgreSQL stores immutable metadata, provenance and SHA-256 digests. PoC objects use never-overwrite keys because Railway buckets do not currently provide object versioning or object lock.
- (direction) Research favors Pino structured JSON logs plus Sentry managed error/performance monitoring for the PoC; product audit events remain immutable database records, never operational logs. Keep correlation identifiers across web, job, Run, provider and evidence activity.
- (direction) Research favors Vercel AI SDK 7 behind an application-owned ModelGateway port; current providers and models remain configuration, with provider/model/version recorded per Run. Benchmark the bounded ProdConsole acquisition before selecting the initial default model.
- (decision) [ADOPTED by user] Name the paradigm ports-and-adapters modular monolith with durable asynchronous Audit Runner execution; do not describe the system broadly as event-driven.
- (decision) [ADOPTED by user] Enforce dependency direction domain <- application <- infrastructure <- apps/composition roots. Domain has no outward dependencies; application depends only on domain and inward-owned ports; infrastructure implements those ports; apps/web and apps/worker wire implementations.
- (constraint) Business and application code must not import Drizzle, pg-boss, Solari, Vercel AI SDK, S3 client, Railway, Sentry, or other provider/framework types.
- (decision) [ADOPTED by user] The architectural center is Procedure -> Run -> Evidence Acquisition and Evidence Evaluation -> Evidence Package and Control Result -> Auditor Review; web, worker and database are delivery/infrastructure concerns around this model.
- (decision) [ADOPTED by user] Use durable asynchronous Run execution through pg-boss, but require idempotent stages and persisted checkpoints; queue delivery claims never imply exactly-once business side effects.
- (decision) Create one durable pg-boss job per Run; the worker executes an application-owned ordered stage plan and resumes at the first incomplete checkpoint after retry or restart.
- (decision) [ADOPTED by user] Where practical, create or transition a Run and enqueue its work atomically in one PostgreSQL transaction.
- (decision) [ADOPTED by user] Define an application-owned EvidenceAcquisition boundary; controlled web acquisition uses a narrower BrowserExecution port implemented by Solari. File, API, future desktop and future private-runner adapters remain external to the audit core.
- (decision) [ADOPTED by user] Railway Buckets are synthetic-PoC evidence storage only. Use unique never-reused keys, expose no overwrite path, record SHA-256 in PostgreSQL, make captured evidence metadata immutable, and verify integrity on consumption/export. This is tamper-evident application enforcement, not WORM storage.
- (decision) [ADOPTED by user] Commercial/customer evidence storage is a mandatory pre-production revisit because Railway Buckets currently lack object versioning, object lock and S3-style server-side encryption.
- (decision) [ADOPTED by user] Bind runtime majors only: Node.js 24 LTS, Next.js 16 and PostgreSQL 18; bootstrap and continuously patch to the latest compatible security-supported releases rather than treating minor/patch versions as architecture.
- (decision) [ADOPTED by user] Better Auth owns authentication and session management only. IntelliFin Audit application/domain roles Auditor, Reviewer/Audit Manager and Admin are authoritative for product permissions.
- (decision) [ADOPTED by user] Vercel AI SDK may implement ModelGateway but no AI SDK types cross inward. Model choice is empirical PoC configuration, benchmarked on IAM-001 and adversarial cases using gpt-5.6-sol, gpt-5.6-terra and claude-sonnet-5; optimize first for bounded success and safe failure.
- (decision) [ADOPTED by user] Record provider/model identity, model configuration, procedure and prompt versions, tool activity, and execution limits for every agentic execution.
- (decision) [ADOPTED by user] Accept Drizzle with explicit migrations, pnpm workspaces without Turborepo, Railway PoC hosting, Pino plus Sentry plus product audit events, and Vitest plus real-PostgreSQL integration tests plus Playwright.
- (decision) [ADOPTED by user] The architecture succeeds when Railway, pg-boss, Better Auth, Drizzle, Solari, model provider, or Next.js can be replaced without redefining what an Audit Run means.
- (version) Verified 2026-09-01 implementation seed additions: @solarisdk/browser 0.1.2, @solarisdk/sandbox 0.1.2, and @aws-sdk/client-s3 3.1123.0.
- (event) Initial architecture spine distilled from accepted constraints and research; awaiting input reconciliation and reviewer gate.
- (decision) Input reconciliation tightened Procedure Version as an approved frozen execution contract; Run dispatch rejects unapproved versions and any logic/source/evidence/model/tool configuration change mints a new version.
- (decision) Input reconciliation tightened source acquisition to read-only least-privilege credentials scoped by Procedure, Source and environment, with no mutation port.
- (decision) Input reconciliation added deterministic Exception fingerprints, linked reruns, declared version compatibility and explicit non-comparable results to support trustworthy change detection.
- (decision) Input reconciliation added exact Run/Review/Exception state machines, expected-revision mutation preconditions, formal review eligibility and irreversible finalized-state immutability.
- (decision) Input reconciliation expanded canonical provenance to original/normalized values, match keys, transformations, ambiguity, exclusions and unevaluated state; addendum H remains authoritative for failure classification.
- (decision) Input reconciliation added authorized application-mediated Evidence reads, inert rendering of untrusted content and pre-persistence/display/export trace redaction.
- (decision) Input reconciliation added an audit-event hash chain and an Ed25519-signed final integrity manifest so privileged relational mutation is detectable and Workpaper Bundles are independently verifiable without live Sources.
- (direction) Railway states storage-level/customer-data encryption at rest; lack of S3-style SSE is an API control limitation, not evidence that storage is plaintext. Do not add custom envelope encryption for synthetic PoC data; revisit customer-controlled keys before production.
- (decision) Input reconciliation bound one active Run per Procedure Version/effective period, linked non-overwriting reruns, cooperative cancellation preserving partial Evidence, at most three bounded Source retries, and no duplicate Results.
- (decision) Input reconciliation added PoC thesis metrics as structured product data and bound operational acceptance targets, accessibility, daily backups, restore testing, retention and documented teardown.
- (decision) Reviewer gate closed cross-store crash consistency with application-reserved artifact idempotency keys, conditional unique object creation, verified registration, seal preconditions, retry reconciliation and orphan/missing-object inventory.
- (decision) Reviewer gate made atomic publication mandatory: CompleteRun commits Gate decision, Result, Exceptions, terminal Run state, checkpoint and audit events in one PostgreSQL UnitOfWork; pg-boss dispatch shares the transaction and future dispatchers require an outbox.
- (decision) Reviewer gate versioned canonical integrity bytes using RFC 8785 UTF-8 JSON, per-aggregate serialized hash chains, SHA-256, Ed25519 signature envelopes, retained public trust bundle, golden vectors and an explicit PoC threat boundary.
- (decision) Reviewer gate introduced CredentialProvider, ManifestSigner, application-owned failure/retry policy, provider behavioral conformance, network request interception, and Procedure-versioned model data policy.
- (decision) Reviewer gate introduced versioned wire schemas/upcasters and canonical provenance graph semantics for jobs, checkpoints, Evidence observations, audit events, manifests and Workpaper Bundles.
- (decision) Reviewer gate introduced expand-migrate-deploy-drain-contract release discipline, release-pipeline-only migrations, startup compatibility checks and durable-job rollback constraints.
- (decision) Reviewer gate defined the recoverable unit as PostgreSQL plus sealed Evidence copy/inventory plus verification keys, with a separately credentialed recovery bucket and end-to-end restore verification.
- (decision) Technology gate chose direct @ai-sdk/openai and @ai-sdk/anthropic adapters for the PoC benchmark rather than Vercel AI Gateway, and removed unused Solari sandbox SDK.
- (decision) Technology gate bound Railway PostgreSQL major 18 through ghcr.io/railwayapp-templates/postgres-ssl:18 and server_version verification while keeping minor patches platform-current.
- (decision) Technology gate bound an allowlist telemetry sanitizer, static Pino redaction, Sentry default-PII/AI-payload disabling and security-negative tests.
- (version) Verified 2026-09-01 additional seeds: @ai-sdk/openai 4.0.53, @ai-sdk/anthropic 4.0.46, @sentry/node 10.73.0.
- (event) Input reconciliation and first independent reviewer gate completed; clear critical/high findings applied to the spine.
- (event) Independent rechecks passed with no remaining critical/high findings: rubric/integrity PASS, technology reality PASS, adversarial divergence PASS.
- (event) spine finalized
