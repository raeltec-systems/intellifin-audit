---
scope: IntelliFin Audit exploratory PoC and reusable commercial-product foundations
purpose: build-substrate
altitude: feature
updated: 2026-09-01T20:59
---

- (decision by user) Use the Coaching path and produce only the terse build-oriented architecture spine.
- (constraint) Greenfield repository: no application code or existing technical conventions are present to ratify.
- (constraint) PoC is a web application with background Audit Runners, four preconfigured Procedures, synthetic data, and read-only Source access.
- (constraint) Deterministic rules own record classification and immutable System Outcomes; agentic behavior is bounded to controlled acquisition or extraction and must fail safely.
- (constraint) Evidence provenance, exact population reconciliation, Evidence Quality Gate, immutable Run history, and separate human review state are binding product invariants.
- (constraint) ProdConsole configuration testing is the PoC agentic vertical slice; other Sources use real synthetic file or read-only API acquisition.
- (constraint) Architecture must preserve a future private or customer-hosted Audit Runner boundary without implementing that deployment in the PoC.
- (constraint) UX mental model is Procedure → Run → Evidence → Result → Auditor Review; agent execution detail remains subordinate.
- (decision by user) Adopt TypeScript as the primary language and Next.js for the web UI; Python is permitted only for a compelling isolated data-processing need, not as a distributed default.
- (direction by user) Optimize for a very small human-plus-coding-agent team: speed, clarity, testability, safe modification, and low operational overhead.
- (decision by user) Adopt a modular monolith in a monorepo with separately runnable web and background worker processes; avoid microservices and Kubernetes.
- (decision by user) Audit Runner is a first-class execution component, never synchronous request work; it must support one genuine bounded agentic web-acquisition flow.
- (decision by user) Use Solari as the initial browser/computer-use/sandbox provider behind a clean provider boundary that preserves future private or customer-hosted execution.
- (decision by user) Adopt PostgreSQL and a relational domain model for Procedures, Runs, Evidence, Exceptions, Reviews, and audit history; avoid disposable persistence shortcuts.
- (constraint) PoC deployment is cloud-hosted, container-friendly, synthetic-data-only, publicly demoable, and must support web, PostgreSQL, long-running workers, logs, secrets, and low operational overhead.
- (direction by user) Do not introduce NestJS merely from familiarity; use a separate lightweight TypeScript backend only if runner boundaries require it.
- (constraint) Manual Runs only; four preconfigured Procedures; read-only Sources; deterministic evaluation authoritative; uncertain agent or Evidence states fail safely.
- (question) Research and decide: hosting, background jobs, authentication, ORM/data access, LLM provider boundary, Next.js-only versus separate API, monorepo tooling, and observability.
- (version) Verified 2026-09-01 paved-path baseline: Node.js 24 LTS; Next.js 16.3.4; React 19.2.8; PostgreSQL 18.6; pnpm 11.25.0.
- (direction) Research favors a ports-and-adapters modular monolith: Next.js is an inbound web adapter and composition root; a separate Node worker is an inbound job adapter; shared domain/application packages remain framework-independent; no separate HTTP API service for the PoC.
- (direction) Research favors pg-boss 12.29.0 for PoC jobs: durable PostgreSQL-backed execution, retries, heartbeats, concurrency and transactional enqueue without Redis or an external workflow control plane; mitigate library/at-least-once side-effect risk with application idempotency and explicit stage checkpoints.
- (direction) Research favors Drizzle ORM 0.45.2 plus drizzle-kit 0.31.10 and explicit SQL migrations: TypeScript schema, visible SQL, low runtime weight, and less generated-client/tooling surface than Prisma 8's newly changed migration path.
- (direction) Research favors Better Auth 1.7.2 with PostgreSQL-backed sessions for PoC portability; keep product authorization roles and review permissions in the application domain rather than coupling them to an identity vendor.
- (direction) Research favors pnpm workspaces without Turborepo initially: two apps and a few shared packages do not yet justify a second task-orchestration layer; add one only when build graph or cache cost becomes material.
- (direction) Research favors Railway for the PoC: native Next.js/Docker, monorepo services, long-running processes, managed PostgreSQL, secrets, logs, public domains, and private S3-compatible buckets in one low-operations project; preserve Docker and S3/PostgreSQL portability.
- (direction) Evidence binary artifacts should use private S3-compatible object storage behind an EvidenceObjectStore port; PostgreSQL stores immutable metadata, provenance and SHA-256 digests. PoC objects use never-overwrite keys because Railway buckets do not currently provide object versioning or object lock.
- (direction) Research favors Pino structured JSON logs plus Sentry managed error/performance monitoring for the PoC; product audit events remain immutable database records, never operational logs. Keep correlation identifiers across web, job, Run, provider and evidence activity.
- (direction) Research favors Vercel AI SDK 7 behind an application-owned ModelGateway port; current providers and models remain configuration, with provider/model/version recorded per Run. Benchmark the bounded ProdConsole acquisition before selecting the initial default model.
- (decision) [ADOPTED by user] Name the paradigm ports-and-adapters modular monolith with durable asynchronous Audit Runner execution; do not describe the system broadly as event-driven.
- (decision) [ADOPTED by user] Enforce dependency direction domain <- application <- infrastructure <- apps/composition roots. Domain has no outward dependencies; application depends only on domain and inward-owned ports; infrastructure implements those ports; apps/web and apps/worker wire implementations.
- (constraint) Business and application code must not import Drizzle, pg-boss, Solari, Vercel AI SDK, S3 client, Railway, Sentry, or other provider/framework types.
- (decision) [ADOPTED by user] The architectural center is Procedure -> Run -> Evidence Acquisition and Evidence Evaluation -> Evidence Package and Control Result -> Auditor Review; web, worker and database are delivery/infrastructure concerns around this model.
- (decision) [ADOPTED by user] Use durable asynchronous Run execution through pg-boss, but require idempotent stages and persisted checkpoints; queue delivery claims never imply exactly-once business side effects.
- (decision) Create one durable pg-boss job per Run; the worker executes an application-owned ordered stage plan and resumes at the first incomplete checkpoint after retry or restart.
- (decision) [ADOPTED by user] Where practical, create or transition a Run and enqueue its work atomically in one PostgreSQL transaction.
- (decision) [ADOPTED by user] Define an application-owned EvidenceAcquisition boundary; controlled web acquisition uses a narrower BrowserExecution port implemented by Solari. File, API, future desktop and future private-runner adapters remain external to the audit core.
- (decision) [ADOPTED by user] Railway Buckets are synthetic-PoC evidence storage only. Use unique never-reused keys, expose no overwrite path, record SHA-256 in PostgreSQL, make captured evidence metadata immutable, and verify integrity on consumption/export. This is tamper-evident application enforcement, not WORM storage.
- (decision) [ADOPTED by user] Commercial/customer evidence storage is a mandatory pre-production revisit because Railway Buckets currently lack object versioning, object lock and S3-style server-side encryption.
- (decision) [ADOPTED by user] Bind runtime majors only: Node.js 24 LTS, Next.js 16 and PostgreSQL 18; bootstrap and continuously patch to the latest compatible security-supported releases rather than treating minor/patch versions as architecture.
- (decision) [ADOPTED by user] Better Auth owns authentication and session management only. IntelliFin Audit application/domain roles Auditor, Reviewer/Audit Manager and Admin are authoritative for product permissions.
- (decision) [ADOPTED by user] Vercel AI SDK may implement ModelGateway but no AI SDK types cross inward. Model choice is empirical PoC configuration, benchmarked on IAM-001 and adversarial cases using gpt-5.6-sol, gpt-5.6-terra and claude-sonnet-5; optimize first for bounded success and safe failure.
- (decision) [ADOPTED by user] Record provider/model identity, model configuration, procedure and prompt versions, tool activity, and execution limits for every agentic execution.
- (decision) [ADOPTED by user] Accept Drizzle with explicit migrations, pnpm workspaces without Turborepo, Railway PoC hosting, Pino plus Sentry plus product audit events, and Vitest plus real-PostgreSQL integration tests plus Playwright.
- (decision) [ADOPTED by user] The architecture succeeds when Railway, pg-boss, Better Auth, Drizzle, Solari, model provider, or Next.js can be replaced without redefining what an Audit Run means.
- (version) Verified 2026-09-01 implementation seed additions: @solarisdk/browser 0.1.2, @solarisdk/sandbox 0.1.2, and @aws-sdk/client-s3 3.1123.0.
- (event) Initial architecture spine distilled from accepted constraints and research; awaiting input reconciliation and reviewer gate.
- (decision) Input reconciliation tightened Procedure Version as an approved frozen execution contract; Run dispatch rejects unapproved versions and any logic/source/evidence/model/tool configuration change mints a new version.
- (decision) Input reconciliation tightened source acquisition to read-only least-privilege credentials scoped by Procedure, Source and environment, with no mutation port.
- (decision) Input reconciliation added deterministic Exception fingerprints, linked reruns, declared version compatibility and explicit non-comparable results to support trustworthy change detection.
- (decision) Input reconciliation added exact Run/Review/Exception state machines, expected-revision mutation preconditions, formal review eligibility and irreversible finalized-state immutability.
- (decision) Input reconciliation expanded canonical provenance to original/normalized values, match keys, transformations, ambiguity, exclusions and unevaluated state; addendum H remains authoritative for failure classification.
- (decision) Input reconciliation added authorized application-mediated Evidence reads, inert rendering of untrusted content and pre-persistence/display/export trace redaction.
- (decision) Input reconciliation added an audit-event hash chain and an Ed25519-signed final integrity manifest so privileged relational mutation is detectable and Workpaper Bundles are independently verifiable without live Sources.
- (direction) Railway states storage-level/customer-data encryption at rest; lack of S3-style SSE is an API control limitation, not evidence that storage is plaintext. Do not add custom envelope encryption for synthetic PoC data; revisit customer-controlled keys before production.
- (decision) Input reconciliation bound one active Run per Procedure Version/effective period, linked non-overwriting reruns, cooperative cancellation preserving partial Evidence, at most three bounded Source retries, and no duplicate Results.
- (decision) Input reconciliation added PoC thesis metrics as structured product data and bound operational acceptance targets, accessibility, daily backups, restore testing, retention and documented teardown.
- (decision) Reviewer gate closed cross-store crash consistency with application-reserved artifact idempotency keys, conditional unique object creation, verified registration, seal preconditions, retry reconciliation and orphan/missing-object inventory.
- (decision) Reviewer gate made atomic publication mandatory: CompleteRun commits Gate decision, Result, Exceptions, terminal Run state, checkpoint and audit events in one PostgreSQL UnitOfWork; pg-boss dispatch shares the transaction and future dispatchers require an outbox.
- (decision) Reviewer gate versioned canonical integrity bytes using RFC 8785 UTF-8 JSON, per-aggregate serialized hash chains, SHA-256, Ed25519 signature envelopes, retained public trust bundle, golden vectors and an explicit PoC threat boundary.
- (decision) Reviewer gate introduced CredentialProvider, ManifestSigner, application-owned failure/retry policy, provider behavioral conformance, network request interception, and Procedure-versioned model data policy.
- (decision) Reviewer gate introduced versioned wire schemas/upcasters and canonical provenance graph semantics for jobs, checkpoints, Evidence observations, audit events, manifests and Workpaper Bundles.
- (decision) Reviewer gate introduced expand-migrate-deploy-drain-contract release discipline, release-pipeline-only migrations, startup compatibility checks and durable-job rollback constraints.
- (decision) Reviewer gate defined the recoverable unit as PostgreSQL plus sealed Evidence copy/inventory plus verification keys, with a separately credentialed recovery bucket and end-to-end restore verification.
- (decision) Technology gate chose direct @ai-sdk/openai and @ai-sdk/anthropic adapters for the PoC benchmark rather than Vercel AI Gateway, and removed unused Solari sandbox SDK.
- (decision) Technology gate bound Railway PostgreSQL major 18 through ghcr.io/railwayapp-templates/postgres-ssl:18 and server_version verification while keeping minor patches platform-current.
- (decision) Technology gate bound an allowlist telemetry sanitizer, static Pino redaction, Sentry default-PII/AI-payload disabling and security-negative tests.
- (version) Verified 2026-09-01 additional seeds: @ai-sdk/openai 4.0.53, @ai-sdk/anthropic 4.0.46, @sentry/node 10.73.0.
- (event) Input reconciliation and first independent reviewer gate completed; clear critical/high findings applied to the spine.
- (event) Independent rechecks passed with no remaining critical/high findings: rubric/integrity PASS, technology reality PASS, adversarial divergence PASS.
- (event) spine finalized
- (event) Update run 2026-09-01: PRD revision 2 (final) and UX spines DESIGN.md/EXPERIENCE.md (final) supersede the revision-1 inputs this spine was distilled from. AD IDs kept stable; rules amended in place; new ADs appended. Adopted technology decisions retained unless revision 2 invalidates them.
- (constraint) PRD rev 2 binds: Procedure Builder with Templates, structured fields and read-only re-deriving plan preview (FR-4..12); Audit Manager approval of Procedure Versions with version states DRAFT/SUBMITTED/APPROVED/REJECTED/ACTIVE/RETIRED, platform-authored drafts on model/prompt/tool/registration change, Regression Run before ACTIVE (FR-13..15); Schedules with derived periods and handover at period boundary (FR-11, FR-17); eight Run states adding PAUSED and AWAITING_AUDITOR (FR-18); Agent Workspace only for agent-driven Steps (FR-19); Session Steps, Work Items, Step Executions, Tool Actions (FR-20..23); Live View, pause/resume, cancel, platform-raised typed Escalations with closed answers, in-app plus email notification (FR-24..28); Timeline written live and platform-owned Replay asset set (FR-29..30); Structural Snapshot grounding, identity/label/value corroboration, Absence Observations from Tool-Action-derived queries (FR-31..33); per-condition evaluation with applicability predicates, Agent-Judged confirmation, Result sealing, COMPLETED->INCONCLUSIVE only at sealing (FR-36..40); Exception assignment and fingerprints (FR-41..42); export of any terminal Run (FR-46).
- (constraint) UX spines bind: DESIGN.md eight state families and one info-solid needs-a-human treatment; EXPERIENCE.md 13 surfaces including Version review, Live View, Replay, Notifications, Administration registrations; Live View state within 5 s and stale indicator at 15 s; no free text reaches the agent; Escalation answers are closed sets rendered in FR-27 order; data tables and column sets; role gating table.
- (decision) AD-2 amended: Procedure Versions are authored by Auditors from Templates in the Builder and approved by a non-author Audit Manager; the version freezes plan, Population Source binding, Target System registration digests, condition applicability predicates, confidence threshold, model/prompt/tool configuration, limits and Schedule; platform-authored drafts are minted by the application on model/prompt/tool/registration change; a Regression Run gates ACTIVE when configuration digests differ; Templates are data owned by the procedures module.
- (decision) AD-3 amended: the worker executes an ordered plan of Session Steps and Work Items with Step Executions and Tool Actions; every Tool Action appends a Timeline event before its effect is observable elsewhere; per-Observation Gate checks and the deterministic evaluator run at Observation registration inside the worker; Run-level Gate runs after the last Work Item and decides COMPLETED vs INCONCLUSIVE; CompleteRun commits the Gate decision, unsealed Result and Timeline head atomically; sealing is a separate SealResult unit of work in the web process.
- (decision) New AD-16 (human-in-the-loop waits): PAUSED and AWAITING_AUDITOR are durable Run states, not in-memory waits; the worker checkpoints, releases the pg-boss job with a timeout-driven wake, and keeps the Agent Workspace alive up to the timeout; a human answer or resume is an application command that enqueues a resume job carrying the Escalation answer id; the agent receives only the chosen option identifier, never free text; timeouts are enforced by the worker on wake, never by the web process.
- (decision) New AD-17 (live supervision channel): the Timeline in PostgreSQL is the single source of live state; the worker emits a NOTIFY per appended Timeline event and the web process fans out to Live View clients over Server-Sent Events with the last-seen event sequence as cursor; clients reconcile by replaying events after their cursor; no second real-time store; Replay frames are the same Replay asset set, so live and replay share one renderer.
- (decision) New AD-18 (two acquisition paths, one Observation contract): Adapters (file, API) and the Audit Agent (web, desktop) both produce Observations in the application-owned schema with grounding into a Structural Snapshot or file Evidence item; Structural Snapshots and screenshots are captured by the platform's BrowserExecution/DesktopExecution adapter at the Tool Action that read the attributes, never by the model; the corroboration extractor is a domain-owned deterministic function over stored snapshots; Absence Observation query strings are derived from the sanitized Tool Action log.
- (decision) New AD-19 (schedules): Schedules are application-owned data executed by a worker-side scheduler that enqueues one Run per due period idempotently keyed by (Procedure Version, period); missed starts are recorded as events; handover between versions happens at the first period boundary after ACTIVE; pg-boss cron is not the source of truth.
- (decision) New AD-20 (notification): NotificationSender is an inward port; the application records each notification and its delivery outcome as audit events; in-app notifications are rows the web reads, email goes through one provider adapter; notification content carries no Evidence values or secrets.
- (decision) AD-4 amended: BrowserExecution and a new DesktopExecution port cover agent-driven Target Systems; both must expose structural snapshot capture, identity of the focused record, and sanitized Tool Action logging as part of their conformance contract; Solari browser and sandbox SDKs implement them for the PoC.
- (decision) AD-6 amended: evaluation is per condition with applicability predicates compiled at approval; Rule-Classified evaluations are produced incrementally by the deterministic evaluator at Observation registration; Agent-Judged evaluations are model output stored with origin AGENT_JUDGED and confirmation pending; Result sealing computes the System Outcome once all confirmations resolve; Unevaluated is a value with an origin.
- (decision) AD-7 amended: Auditor Review states DRAFT/SUBMITTED/APPROVED/FINALIZED with rejection as an event; Exceptions gain assignment; Procedure Version states added; expected-revision preconditions extend to confirmations and Escalation answers.
- (decision) AD-9 amended: Escalations are typed application objects with closed answer sets raised by the platform (unnamed value, retry or skip) or the agent (choose candidate only when platform resolution fails); the model never receives free text from humans; agent narration and questions are stored as untrusted content.
- (decision) AD-14 amended: new versioned durable contracts for Timeline events, Replay assets, Structural Snapshots, Escalations and answers, notification records, Procedure Templates and compiled plans.
- (decision) Modules amended: procedures gains builder and templates; runs gains supervision (pause, escalation, live channel) and schedules; evidence gains snapshots and corroboration; new notifications module. Web adds an SSE route handler and a Live View composition; worker adds scheduler and workspace lifecycle.
- (assumption) Email transport for FR-28 is a provider adapter behind NotificationSender; provider choice is seed, [ASSUMPTION] Resend, verified on the web before pinning.
- (assumption) Desktop Target System (LedgerDesk) runs in the Solari sandbox desktop; @solarisdk/sandbox re-added to the seed pending web verification of a desktop/computer-use surface.
- (assumption) Live channel uses PostgreSQL LISTEN/NOTIFY plus SSE rather than a WebSocket server or Redis pub/sub, to stay within the modular-monolith and single-database decisions; revisit if fan-out exceeds one web instance.
- (version) Verified 2026-09-01 (npm registry): @solarisdk/sandbox 0.1.2 describes ephemeral compute sandboxes with optional desktops over a computer-use action API; resend 6.25.0; pg-boss 12.29.0 includes cron-parser (cron schedules exist but are not the Schedule source of truth per AD-19).
- (question) Desktop Structural Snapshot: the Solari sandbox desktop exposes a computer-use action API (screenshot, mouse, keyboard); an accessibility/control tree (AT-SPI) on that desktop is unconfirmed. AD-18 requires the synthetic LedgerDesk app to expose a control-tree snapshot (AT-SPI or an in-app structured snapshot endpoint) so account_status stays Rule-Classified; if neither is possible, LedgerDesk attributes are declared model-read and the hero Procedure's LedgerDesk conditions become Agent-Judged. Owner: Engineering. Revisit: when LedgerDesk is built (PRD Open Questions 4 and 8).
- (decision) Provider seeds: Resend 6.25.0 behind NotificationSender for email; @solarisdk/sandbox 0.1.2 behind DesktopExecution; @solarisdk/browser 0.1.2 behind BrowserExecution.
- (decision) Rubric gate rev 2: new AD-21 makes SealResult one application command invoked by CompleteRun when nothing is pending or by the last confirmation otherwise; Run state machine copied from addendum E with deadline, cancel-from-active, and RUN_FAILED transitions; confidence threshold bound in AD-6; Reference Sources acquired as Adapter Session Steps and registered as artifacts; new registrations module owns Target System registrations, bindings, credential refs, digests, and draft-minting change events; evaluation owns Exception creation and fingerprints, review owns a disposition/assignment aggregate keyed by Exception id; checkpoints carry the workspace lease identity and a lost workspace re-runs sign-in under the Session Step retry budget; Workpaper Bundle bound to one archive with signed manifest.json, keys/, artifacts/<sha256>, and versioned JSON members; AD-22 splits the audit-chain and signing rules out of AD-5.
- (version) Stack rows TypeScript 7.0.2, postgres.js 3.4.9, Zod 4.5.4, Pino 10.3.1, Vitest 4.1.11, Playwright 1.62.1, @sentry/nextjs 10.73.0, ai 7.0.87, Node 24.20.0 were verified in the revision-1 technology-reality review and are carried forward.
- (decision) Reconciliation and technology gate rev 2 applied: Regression Runs may reference the APPROVED version they gate; approval starts them when digests differ, the approver confirms from the Template's confirmation script, golden expectations are versioned Template data mirrored into tests; new AD-23 PlanCompiler owns deterministic compilation with compiler version, derivation model identity and re-derivation records, plus Builder validation invariants; execution adapters suppress capture during credential entry and redact secret inputs from artifacts; desktop control tree comes from an in-VM snapshot agent on a project-owned Solari template (Solari is transport only); per-Observation vs Run-level Gate membership fixed with condition completeness at Run level; model-read compiled conditions recorded AGENT_JUDGED; compatibility declared at approval per addendum B; masking designation on the binding with audited unmasked reads; Escalation answer sets enumerated with the no-evaluation invariant; closed outcome mapping bound in AD-3 with Gate outcomes as Timeline events and batched Adapter registration; wait deadlines 30 min / 4 h with complete-plus-deferred-send and a lease reaper; SSE heartbeat 30 s, lifetime under 15 min, LISTEN teardown, subscribing surfaces named; Work Item cardinality allows per-page agent Work Items; one event store with Observation digests in registration events; version-lifecycle notifications in-app; sealing precedence per addendum E.1; artifact kinds enumerated including uploads and recording segments; diagnostics rows written by the worker; TypeScript 7 workspace caveats; seed bumps ai 7.0.89, openai 4.0.55, anthropic 4.0.47, S3 3.1124.0.
- (question) Addendum B.1 lists UNEVALUATED as an evaluation origin; the spine and UX spines treat it as a value with an origin. Offer to update addendum B.1 so upstream matches. Owner: Product. Revisit: next PRD touch.
- (decision) Adversarial gate rev 2 applied: one durable job per wait with singleton key and a locked wait-record closure command (ADV2-01); absence evidence defined per capture_method with Adapter Actions sharing the sanitized-action schema (ADV2-02); identity grounded in the same snapshot as values, separate match provenance node, secondary-key check for human-matched (ADV2-03); Run kind STANDARD/REGRESSION with Regression semantics on the version (ADV2-04); handover_at computed at approval, uniqueness on (Procedure, period), scheduler retires the predecessor (ADV2-05); Exceptions created at first EXCEPTION evaluation with counts_toward_outcome at sealing, review only after sealing (ADV2-06); confirmations fenced to COMPLETED and serialized on the Result row lock (ADV2-07); predicate language is an AD-14 contract with fail-closed semantics (ADV2-08); Unevaluated at the Run-level Gate is Inconclusive unless HUMAN origin (ADV2-09); resume restarts the Step Execution as a new attempt with attach/release conformance (ADV2-10); Timeline seq is the AD-22 chain sequence under the head lock (ADV2-11); artifact roles required vs replay (ADV2-12); canonical registration digest field set and comparison baseline (ADV2-13); SealPackage in every terminal transition and cancel_requested with one CANCELED writer per state (ADV2-14); snapshot substrate kinds and label rules (ADV2-15); Notifications as a query over open waits (ADV2-16); plan derivation as a queued job and the worker never re-derives (ADV2-17); once Schedules manual-only; Reference Sources acquired at Run start.
- (event) spine finalized (revision 2)
- (decision) Desktop control tree resolved: LedgerDesk serves its control tree as JSON on localhost inside the Solari VM; the in-VM snapshot agent reads that endpoint (no AT-SPI). Binds AD-4 snapshot source for desktop; prevents per-app extractor divergence.
- (decision) Provider recording handling resolved: copy recording into platform object storage at Run end (SealPackage input), provider retention at minimum, region matches Railway region. Confirms AD-9 Replay asset ownership.
- (version) Default model for plan derivation and Agent-Judged evaluation: claude-sonnet-5 via @ai-sdk/anthropic; @ai-sdk/openai remains a wired fallback; identity recorded per Procedure Version.
