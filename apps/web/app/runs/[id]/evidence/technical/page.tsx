/**
 * Compatibility route for technical Evidence links. The record-first queue owns the
 * ordinary Evidence route; this route keeps the bounded artifact/grounding surface and
 * its stable evidence anchors addressable for existing Result and escalation links.
 */
export { default, metadata } from './artifacts/page';
export const dynamic = 'force-dynamic';
