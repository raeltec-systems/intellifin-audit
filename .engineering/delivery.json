{"base":"663c959a1b66b0463c2ac4c209cb89225d7d614d","commits":[{"message":"fix(workspace): retain provider identity across worker configuration changes","edits":[{"path":"packages/application/src/runs/provision-workspace.ts","old":"      mode: deps.browser.mode,","new":"      // Existing identity includes its provider; deployment configuration cannot relabel it.\n      mode: prior?.workspaceId != null ? prior.mode : deps.browser.mode,"},{"path":"packages/application/src/runs/provision-workspace.ts","old":"  let diagnostic: WorkspaceDiagnostic = 'workspace-created';\n  try {","new":"  let diagnostic: WorkspaceDiagnostic = 'workspace-created';\n  try {\n    // Keep the cleanup reference intact when this worker cannot operate its provider.\n    if (checkpoint.workspaceId !== null && checkpoint.mode !== deps.browser.mode) {\n      throw new WorkspaceProvisionError('policy');\n    }"},{"path":"packages/application/src/runs/provision-workspace.test.ts","old":"describe('provisionWorkspace', () => {\n","new":"describe('provisionWorkspace', () => {\n  it.each([['solari', 'local'], ['local', 'solari']] as const)(\n    'preserves the persisted %s identity when a restarted worker selects %s',\n    async (originalMode, configuredMode) => {\n      const state = store(agentPlan());\n      const original = new FakeBrowser({ mode: originalMode, expiresAt: '2026-09-07T00:00:00.000Z' });\n      await provisionWorkspace(DEPS(state, original), JOB);\n      const identity = state.checkpoint!.workspaceId;\n      const restarted = new FakeBrowser({ mode: configuredMode });\n      expect(await provisionWorkspace(DEPS(state, restarted), JOB)).toEqual({ retry: false, provisioned: false });\n      expect(restarted.created).toEqual([]);\n      expect(restarted.attached).toEqual([]);\n      expect(restarted.released).toEqual([]);\n      expect(state.checkpoint).toMatchObject({ workspaceId: identity, mode: originalMode, status: 'FAILED', diagnostic: 'workspace-policy' });\n      expect(state.run?.state).toBe('RUN_FAILED');\n    },\n  );\n\n"},{"path":"CLAUDE.md","old":"# CLAUDE.md\n","new":"# CLAUDE.md\n\n## Engineering continuation — workspace identity\n\nAn existing workspace is identified by provider mode plus provider session ID. Never overwrite its mode from current worker configuration. A provider change must preserve the old cleanup reference and refuse continuation; a new Run can use the new provider. The regression covers both Solari-to-local and local-to-Solari restarts.\n"}]}]}
